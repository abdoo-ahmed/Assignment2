# Node.js Assignment 2

## 📌 Description
This repository contains solutions for Node.js assignment tasks using core Node modules such as `path`, `fs`, `events`, and `os`.  
The assignment focuses on working with file system operations, paths, events, and system information.

---

## 🧩 Tasks Overview

### 📂 Path Module
The assignment includes multiple tasks using the **path** module:

- Get current file path and directory path
- Extract file name from a path
- Build a full path from divided path parts
- Get file extension
- Parse path and return name and extension
- Check if a path is absolute
- Join multiple path parts
- Resolve relative paths
- Join two paths together

---

### 📁 File System (fs) Module

Tasks using **fs** and **fs.promises**:

- Delete a file asynchronously
- Create a folder synchronously
- Read a file synchronously
- Write a file asynchronously
- Check if a directory or file exists

---

### ⚡ Events Module

Tasks using **EventEmitter**:

- Create a custom event and trigger it
- Emit a login event with username parameter

---

### 🖥️ OS Module

Tasks using **os** module:

- Get operating system platform
- Get system architecture (CPU type)

---

## 🚀 Technologies Used
- Node.js
- JavaScript (ES6)

